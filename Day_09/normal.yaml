---
- hosts: all
  gather_facts: false

  tasks:
    - name: Long running task - 1
      ansible.builtin.command: sleep 5
      changed_when: false

    - name: Long running task - 1
      ansible.builtin.command: sleep 5
      changed_when: false

    - name: Long running task - 1
      ansible.builtin.command: sleep 5
      changed_when: false
